import type { ReactElement } from "react";
import { DiscoverPage } from "@/components/campaign/discover-page";

export const metadata = { title: "Discover Campaigns — CreatorLane" };

export default function Page(): ReactElement {
  return <DiscoverPage />;
}
